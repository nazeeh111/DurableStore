[package]
name = "durablestore"
version = "0.1.0"
edition = "2024"
rust-version = "1.89"
description = "An inspectable embedded byte store with durable writes and crash recovery"
license = "MIT"
repository = "https://github.com/nazeeh111/DurableStore"

[features]
# Explicitly enabled only for the crash-test binary; never enable in normal use.
fault-injection = []

[dependencies]
